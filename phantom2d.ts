interface Phantom2dOptions {
    collide?: Function;
    x?: number;
    y?: number;
    rot?: number;
    width?: number;
    height?: number;
    custom?: { any: any };
}

class Phantom2dEntity {
    constructor(expects: string[], objname: string, settings: Phantom2dOptions) {
        //
    }
}